[project]
name = "rave-latent-analysis"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "google-cloud-storage>=2.0",
    "librosa>=0.10",
    "numpy>=1.24",
    "pandas>=2.0",
    "requests>=2.31",
    "resampy>=0.4",
    "soundfile>=0.12",
    "torch>=2.0",
    "torchcrepe>=0.0.20",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0"]
